---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: eventscalepolicies.autoscaling.eventscale.com
spec:
  group: autoscaling.eventscale.com
  names:
    kind: EventScalePolicy
    listKind: EventScalePolicyList
    plural: eventscalepolicies
    singular: eventscalepolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.targetReplicas
      name: TargetReplicas
      type: integer
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EventScalePolicySpec은 친구분과 정의한 핵심 필드들을 포함합니다.
            properties:
              minReplicasAfterEvent:
                format: int32
                minimum: 1
                type: integer
              openTime:
                format: date-time
                type: string
              preScaleSeconds:
                format: int32
                minimum: 0
                type: integer
              restoreAfterSeconds:
                format: int32
                minimum: 0
                type: integer
              targetRef:
                description: TargetRef는 조작할 HPA 리소스를 가리킵니다.
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              targetReplicas:
                format: int32
                minimum: 1
                type: integer
            required:
            - minReplicasAfterEvent
            - openTime
            - preScaleSeconds
            - restoreAfterSeconds
            - targetRef
            - targetReplicas
            type: object
          status:
            description: EventScalePolicyStatus는 오퍼레이터가 관리하는 현재 상태를 기록합니다.
            properties:
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
