apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  namespace: ticketing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
        - name: mysql
          image: mysql:8.4
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root1234
            - name: MYSQL_DATABASE
              value: ticketing
            - name: MYSQL_USER
              value: ticketing
            - name: MYSQL_PASSWORD
              value: ticketing1234
---
apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: ticketing
spec:
  type: ClusterIP
  selector:
    app: mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
